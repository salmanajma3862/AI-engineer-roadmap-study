name = input ("Enter your name: ") # this will take input from the user and store it in the variable name
age = input ("Enter your age: ") # this will take input from the user and store it in the variable age
# lets print the name and age using the variables
print("Hello " + name + ", you are " + age + " years old") # this will print the string "Hello " and then concatenate it with the string stored in the variable name and then concatenate it with the string " years old"
print("Hello " + name) # this will print the string "Hello " and then concatenate it with the string stored in the variable name